<h1>Show Article</h1>
<h1><%=@article.title%></h1>
<p><%=@article.body%></p>
        
<ul>
    <li>
        <%=link_to "Edit", 
            edit_article_path(@article)
        %>
    </li>    
    <li>
        <%=link_to "Destory", article_path(@article), 
            method: :delete, 
            data: { confirm: "are you sure" }%>
    </li>
</ul>

<h2>Comments</h2>
<%# выведет по шаблону всю колекцию %>
<%= render @article.comments %>

<h1>Add Comment</h1>
<%= render "comments/form" %>